# log-analysis

# Log Sorce
https://github.com/d4rk-d4nph3/Windows-Event-Samples

# create venv
python -m venv .venv
source .venv/bin/activate

# install dependencies
pip install -r requirements.txt

